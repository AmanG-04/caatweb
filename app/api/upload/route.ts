import { NextResponse } from "next/server";
import { getEnv } from "@/lib/cloudflare";
import { consumeRateLimit } from "@/lib/rate-limit";
export const runtime = "edge";
const MAX = 10 * 1024 * 1024;
export async function POST(request: Request) { const contentLength=Number(request.headers.get("content-length")??0); if(contentLength>MAX+100000)return NextResponse.json({success:false,error:{code:"PAYLOAD_TOO_LARGE",message:"Upload must be smaller than 10MB."}},{status:413}); const rate=await consumeRateLimit(request,"bill-upload",5,600); if(!rate.allowed)return NextResponse.json({success:false,error:{code:"RATE_LIMITED",message:"Too many upload attempts. Please try again later."}},{status:429,headers:{"Retry-After":String(Math.max(1,rate.reset-Math.floor(Date.now()/1000)))}}); const form = await request.formData(); const file = form.get("file"); if (!(file instanceof File)) return NextResponse.json({success:false,error:{code:"FILE_REQUIRED",message:"A bill file is required."}},{status:400}); const allowed=["application/pdf","image/png","image/jpeg"]; if(!allowed.includes(file.type)||file.size>MAX)return NextResponse.json({success:false,error:{code:"INVALID_FILE",message:"Only PDF, PNG and JPEG files up to 10MB are accepted."}},{status:400}); const objectKey=`bills/${crypto.randomUUID()}-${file.name.replace(/[^a-zA-Z0-9._-]/g,"_")}`; const bucket=getEnv().BILLS_BUCKET; if(!bucket)return NextResponse.json({success:false,error:{code:"STORAGE_UNAVAILABLE",message:"File storage is not configured."}},{status:503}); await bucket.put(objectKey,await file.arrayBuffer(),{httpMetadata:{contentType:file.type}}); return NextResponse.json({success:true,data:{objectKey,message:"Stored securely in R2."}}); }
